export const metadata = {
  title: 'Venues - diyshows beta',
  description: 'Browse DIY venues and spaces',
}

export default function VenuesLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
