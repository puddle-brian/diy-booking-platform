export const metadata = {
  title: 'Admin - diyshows beta',
  description: 'Admin panel for diyshows platform',
}

export default function AdminLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return children;
}
